/** Clases del overlay de `WrappingSelect` para copiar o pasar como `panel-preset` custom vía prop string. */
export const wrappingSelectPanelOverlayClass = {
    filter: '!max-w-[min(12rem,calc(100vw-2rem))] min-w-0 overflow-hidden',
    dialog: '!max-w-[min(32rem,calc(100vw-2rem))] min-w-0 overflow-hidden',
};
