server {
    listen 80;
    server_name evolution.tudominio.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name evolution.tudominio.com;

    # ssl_certificate     /etc/letsencrypt/live/evolution.tudominio.com/fullchain.pem;
    # ssl_certificate_key /etc/letsencrypt/live/evolution.tudominio.com/privkey.pem;

    client_max_body_size 50M;

    location / {
        proxy_pass http://evolution-api:8080;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSockets (manager / realtime)
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }
}
